#!/usr/bin/python3
"""this module defines a function"""


def is_same_class(obj, a_class):
    """returns if object is an instane of class"""
    if isinstance(obj, a_class):
        return True
    else:
        return False
